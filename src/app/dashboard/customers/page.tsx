import React from 'react'
import { geistMono } from '@/app/ui/fonts'
const DashboardInvoices = () => {
  return (
    <div className={`${geistMono.className} font-extralight `}>
          <h1 className={`text-[40px] font-light`}>Customers</h1>
    </div>
  )
}

export default DashboardInvoices
